#include <stdio.h>
#include <stdlib.h>

int main()
{
   int becon = 5.683;
   int becon2 = 5.3;

   printf("becon is \t : %.2f\n", floor(becon));
   printf("becon2 is \t : %.2f\n", floor(becon2));

printf(" becon is %.2f \n", cell(becon2));

    return 0;
}
