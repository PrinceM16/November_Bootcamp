#include <iostream>

int main()
{
int outEdges;

    void outEdges(int i, List &edges)
    {
        for (int j = 0;
         j < n;
        j++) if (a[i][j]) edges.add(j);
         }
    cout << "Hello world!" << endl;
    return 0;
}
